color-scheme = hexo-config("color_scheme")

if color-scheme == "yilia"
    body
        background gray

    .article-header
        border-left 5px solid #666

    .article-more-link a
        background #4d4d4d
        font-size .8em
